<?xml version="1.0" encoding="UTF-8"?>
<?oxygen RNGSchema="http://www.tei-c.org/release/xml/tei/custom/schema/relaxng/tei_all.rng" type="xml"?>
<TEI xmlns="http://www.tei-c.org/ns/1.0">
<teiHeader xml:id="L1916_{{Letter}}">
    <fileDesc>
            <titleStmt>
                <title>{{Title|e}}</title>
                {% if Creator %}
                <author>{{Creator|e}}</author>
                {% else %}
                <author>MISSING</author>
                {% endif %}
                
            </titleStmt>
            <editionStmt>
               <!-- some kind of respStmt or correct thing here... -->
                <!-- Should be written sometime yay! -->
                <edition>
                    The Letters 1916
                </edition>
                <respStmt>
                    <resp>
                        Edited by
                    </resp>
                    <name>Susan Schreibman</name>
        <!-- fix this properly -->
                </respStmt>
            </editionStmt>
            <publicationStmt>
                <publisher>
                    <address>
                        <name>An Foras Feasa</name>
                        <orgName>Maynooth University</orgName>
                        <postBox>Co. Kildare</postBox>
                        <placeName>
                            <country>IRE</country>
                            <settlement>Maynooth</settlement>
                        </placeName>
                    </address>
                </publisher>
                <idno n="L1916_{{Letter|e}}"/>
                <availability status="restricted">
                    <p><!-- Copyright status-specific thing... --></p>
                    
                    <p>All rights reserved. No part of this image may be reproduced, distributed, or
        transmitted in any form or by any means, including photocopying, recording, or other
        electronic or mechanical methods, without the prior written permission of the institutional
        or private owner of the image and the Letters of 1916 project. For permission requests,
        write to the Letters of 1916 at letters1916@gmail.com. </p>
                    <!-- <p>Copyright  2015, Letters of 1916, Maynooth</p> -->
                </availability>
                <date when="2015">2015</date>
            </publicationStmt>
            <!--<seriesStmt>
                 Series statement is for book series
                <p>PUT A STATEMENT</p>
            </seriesStmt>-->
            <notesStmt>
                <note type="summary">
                    <!-- The abstract -->
                    {% if Description %}
                        {% for desc_para in Description.split('\n\n') %}
                            <p>{{desc_para|e}}</p>
                        {% endfor %}
                    {% endif %}
                </note>
            </notesStmt>
            <sourceDesc>
                <msDesc>
                    <msIdentifier>
                        {% if 'Private' in Source %}
                        <repository ref="#{{Source|e}}"></repository>
                        <collection type='private'>{{Source|e}}</collection><!--MANDATORY-->
                        {% else %}
                        <repository ref="#{{Source|e}}">{{Source|e}}</repository>
                        <!-- {% if Document_Collection_Number %}
                                Original DCN: {{Document_Collection_Number|e}}
                             {% endif %}
                        -->
                            {% if Document_Collection %}
                                <collection type='public'>{{Document_Collection|e}}</collection><!--MANDATORY -->
                                <!--check for correctness: use the script which split the string!-->
                            {% endif %}
                            {% if Document_Number|e %}
                        <idno>{{Document_Number|e}}</idno>
                           {% endif %}
                       {% endif %}
                    </msIdentifier>
                </msDesc>
           </sourceDesc>
        </fileDesc>
    <profileDesc>
            <correspDesc>
                

                <correspAction type="received">
                {% if Recipient %}
                    <persName ref="#{{to_snake(Recipient)|e}}">{{Recipient|e}}</persName>
                {% else %}
                    <persName>MISSING</persName>
                {% endif %}
                


                {% if Recipient_s_location %}
                    <placeName  ref="#{{to_snake(Recipient_s_location)|e}}">{{Recipient_s_location|e}}</placeName>
                {% endif %}
                <!-- <date>WE DON'T HAVE A DEVOTED FIELD!!!</date> -->
                </correspAction>
                 
                <correspAction type="sent">
                    {% if Creator %} 
                    <persName ref="#{{to_snake(Creator)|e}}">{{Creator|e}}</persName>
                    {% endif %}
                    {% if Sender_s_location %} 
                    <placeName ref="#{{to_snake(Sender_s_location)|e}}">{{Sender_s_location|e}}</placeName>
                    {% endif %}
                    {% if DATE_created %}
                                        <date when="{{DATE_created}}">{{DATE_created_as_words}}</date>
                    <!-- Make the date human-readable : rcd: strftime-->
                    {% endif %}
                </correspAction>
            </correspDesc>
            <textClass>
                <!-- Type of the letter should be separate -->
                <keywords>
                    <list>
                    {% if Authors_gender %}
                        <item n="gender">{{Authors_gender|e}}</item><!--put an IF -->
                    {% endif %}  
                    {% if Year_of_death_of_author %}
                        <item n="death">{{Year_of_death_of_author|e}}</item>
                        {% endif %}
                    {% if Collection %}    
                        <item n="tag">{{Collection|e}}</item>
                    {% endif %}
                    {% if Topics %}
                        {% for topic in Topics.split(',') %}
                            {% if topic.lower() != Collection.lower() %}
                            <item n="tag">{{topic|e}}</item>
                            {% endif %}
                        {% endfor %}
                    {% endif %}       
                    </list>
                </keywords>
            </textClass>
            <langUsage>
                <language ident="en">{{Language|e}}</language>
            </langUsage>
     </profileDesc>
    <encodingDesc>
        <editorialDecl>
            <p>
                <!-- Write this -->
            </p>
        </editorialDecl>
        <projectDesc>
            <p><!-- Was done by crowdsourcing, Python, magic, etc. --></p>
        </projectDesc>
        <classDecl>
            <taxonomy>
                <listBibl>
                    <bibl>Tags taken from Library of Congress!!! <!-- sort this out --></bibl>
                </listBibl>
            </taxonomy>
        </classDecl>
        <appInfo>
            <application xml:id="smthing" ident="rh" version="1">
                <desc>Richard's Python Editing Tool</desc>
            </application>
        </appInfo>
        <tagsDecl>
            <rendition>
                <!-- Some stuff... -->
            </rendition>
        </tagsDecl>
    </encodingDesc>
    <revisionDesc>
        {% for revision in Edits %}
            <change when="{{revision.datetime}}" who="#{{revision.editor}}">
                {{revision.editType}}
                {% if revision.editType == 'Clean Tags' %}
                   Page {{ revision.page_cleaned }} of {{ revision.clean_count }} errors.
                {% endif %}
            </change>
        {% endfor %}
    </revisionDesc>
</teiHeader>
    
    <facsimile> 
        {% for key, page in sorted(Pages.items()) %}
        <graphic xml:id="L1916_{{Letter}}_img_{{loop.index}}" url="{{page.Archive_Filename}}"/> 
        {% endfor %}
    </facsimile>               

       
         
       
        
        <text type="postcardAM">
            <body>
                {% for key, page in sorted(Pages.items()) %}
                    {% if page.PageType == 'AddressSide'  %}
                       <div type="address" facs="#L1916_{{Letter}}_img_{{loop.index}}">
                            <ab>
                            {{page.Translation}}
                            </ab>
                       </div>
                    {% endif %}
                 {% endfor %}
                 {% for key, page in sorted(Pages.items()) %}    
                    {% if page.PageType == 'TextSide'  %}
                       
                        <div type="message" facs="#L1916_{{Letter}}_img_{{loop.index}}">
                            {{page.Translation}}
                        </div>
                    {% endif %}
                {% endfor %}
                {% for key, page in sorted(Pages.items()) %}     
                    {% if not page.PageType %}
                        <!-- unknown page type --- CHECK!!! -->
                         {% if page.Translation %}
                            {{page.Translation}}
                         {% endif %}
                    {% endif %}
                {% endfor %}
            </body>
        </text>

     

</TEI>


    